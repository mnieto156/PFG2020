/* Precedencia de operadores */

void main() {
	int a;
        int x;

	printc("PRECEDENCIA DE OPERADORES");

	x=50;
      	a= ((x-1) > 3) || 0;

	printc ("true:");
      	if (a || 0) {
		printc(" true");
	}
        
      	else {
        	printc(" false");
	}
}